# Bronze (extração)
requests>=2.32
pandas>=2.2
pyarrow>=16.0
google-cloud-storage>=2.16
google-cloud-bigquery>=3.25

# Spark (Silver pesado) — instalado no ambiente Databricks/Dataproc; aqui p/ dev local
pyspark>=3.5

# GenAI (Fase 4)
google-genai>=1.0          # SDK oficial Gemini (free tier)
numpy>=1.26                # similaridade de cosseno do RAG (catálogo pequeno, in-memory)
# chromadb>=0.5            # vector DB p/ quando o catálogo crescer (opcional)

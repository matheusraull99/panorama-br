# Bronze (extração)
requests>=2.32
pandas>=2.2
pyarrow>=16.0
google-cloud-storage>=2.16
google-cloud-bigquery>=3.25

# Spark (Silver pesado) — instalado no ambiente Databricks/Dataproc; aqui p/ dev local
pyspark>=3.5

# GenAI (Fase 4)
langchain>=0.3
langchain-community>=0.3
chromadb>=0.5
# LLM: escolher um dos provedores
# google-generativeai>=0.8   # Gemini (free tier)
# anthropic>=0.40            # Claude
